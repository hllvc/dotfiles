name: "🚫 _reusable_deploy_prod"

on:
  workflow_call:
    inputs:
      aws-region:
        required: true
        type: string
        description: "AWS Region (eu-central-1 or us-east-2)"
      image-tag:
        required: true
        type: string
        description: "Docker image tag to deploy"

jobs:
  deploy:
    name: "Deploy"
    uses: StackGuardian/sg-internal-github-actions/.github/workflows/_deploy_lambda.yml@main
    with:
      aws-account-id: "476299211833"
      aws-region: ${{ inputs.aws-region }}
      deploy-environment: "PROD"
      ecr-repo: "{{ECR_REPO}}"
      function-name: "{{PROD_FUNCTION_NAME}}"
      image-tag: ${{ inputs.image-tag }}
